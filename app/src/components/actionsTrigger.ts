/** Stable id for the Client Actions trigger. The button moves between the top
 *  nav (home/book context) and the verdict row (account context), but only ONE
 *  is mounted at a time — so a single id lets the panel return focus to
 *  whichever trigger actually opened it (C6). */
export const ACTIONS_TRIGGER_ID = "c360-client-actions-trigger";
