# Handover 2026-08-26 — session close + the 21st.dev design-variant track

Read order for a fresh session: STATUS.md → ROAD-TO-CLOSURE-2026-09-04.md (plan of record, incl.
strategic lanes) → DESIGN-MOD-WORKROOM.md → this file. Evidence: EVIDENCE-SEPT4.md. UAT findings:
uat-findings-20260825.md. All synced: GitHub cowork-offering/customer-360, Banksy clone +
/opt/brain/knowledge/projects/customer-360/, direct SSH remote `banksy` as GitHub fallback.

## Where the product stands (all live, all verified)
- Org: 24 MCP tools, all write pairs wire-proven incl. multi-facility modification (N members, one
  package anchor, one token). Apex suite 177/177 (13 classes). sf CLI auth EXPIRED (runtime
  unaffected — everything runs via the MCP connector; re-auth only needed for the next deploy).
- Cockpit (both artifact URLs, label f02c29b-drift-fix): package-first tickets (PP headline, member
  multiselect, N-of-7 counts), decision-oriented challenge cards computing on LIVE synced figures,
  banker-language gap notes, collateral + covenant context in tickets, drift gate with
  "Refresh figures and re-stage". 1,601/1,601 vitest, contrast gate 54 checks.
- Plugin v0.5.0 in the cowork-offering marketplace: orchestrator agent + 5 guided skills, tool-name
  release gate 24/24. G3 (cold Cowork run) still pending.
- Hartwell: 6 booked + the standing TEST showcase facility (Proposal, Mazak collateral, FCCR
  junction) + workflow texture (Case 00001330, R-8, RG-0000004). Rollback recipes in EVIDENCE.

## The Modification Workroom (the Sept 1 flagship)
Spec: DESIGN-MOD-WORKROOM.md — conversational composer that COMMITS: natural language → LLM parse
(gateway get_llm_response) → deterministic validation vs org fields → proposed delta chips →
one-tap confirm into the live change manifest ("what we have → what this modification changes") →
ONE approval, ONE token → real records, filed state with ids → drafted client reply. Scope: terms,
covenants, collateral, policy exceptions. Package-altitude narrative (founder correction on mock
v1): the story is told at Product Package level, loans are the records layer one expander down;
progressive disclosure everywhere, one decision per scene.
Mock v1 shipped, v2 (package altitude + disclosure) in rework in this session. Links:
- Mock artifact: https://claude.ai/code/artifact/14603d3d-396b-4731-8605-08458c881e94
- No-login: https://bot.connectry.io/s/c360-workroom/ (also /s/c360-board, /s/c360-plan)
Wiring window Aug 27-31 against the founder-blessed mock. Freeze Sept 1. Walkthrough for Steph
Sept 4. Lock Sept 14. Show Sept 15-16.

## The 21st.dev design-variant track (Fabian driving)
Purpose: generate award-grade visual variants of (a) the cockpit and (b) the workroom; we graft the
winning visual system into our build. Setup: MCP server "21st" added to ~/.claude.json on the Archy
box (HTTP, key on file) — verified Connected; tools load in any NEW session (old broken "magic"
npx entry can be removed). A full self-contained generation prompt was handed to Fabian in-session
(cockpit + workroom, real Hartwell figures, Apple-calm register, #A100FF restraint, motion spec,
component inventory, tokens file). Regenerate from DESIGN-MOD-WORKROOM.md + this file if lost.

### Rules for ANY design variant (the graft contract — variants that break these cannot ship)
1. Real figures only, exactly the Hartwell numbers in the mock/spec. No lorem, no invented money.
2. Package altitude: PP narrates, loans one level down. Counts always "N of M".
3. The governance beats are sacred UI moments: proposed chip → explicit confirm; ONE approve
   (single-use token language); org warnings verbatim; filed state shows real record ids +
   "verified by re-query"; Exception ≠ breached; honest gap notes in banker language.
4. Semantic colors muted; #A100FF only for the primary action + brand gesture.
5. Deliverables that graft cleanly: design tokens file + componentized React/Tailwind pieces
   (WorklistRow, StatCell, CovenantRow, FacilityRow, DeltaChip, ManifestEntry, CheckCard,
   ComposerInput, StreamingCard, ApproveBar). We keep our data contracts, guards and wiring;
   variants donate ONLY the visual system.
### Workflow: Fabian generates variants → drops the output (code or link) to the C360 session →
we graft into app/src token layer → full gates (vitest, typecheck, contrast) → republish both URLs.

## Open items (owner)
- Mock v2 review + comment pass (Fabian) → wiring starts Aug 27.
- The approve click from the cockpit UI + G3 cold Cowork run (Fabian).
- WS2 policy pack + ledger (Jon — kickoff still pending, window tight, due Aug 29).
- sf CLI re-auth before next Apex deploy (Fabian, 1 min web login).
- Discussion topics: uat-findings-20260825.md §topics.
- Housekeeping: broken "magic" MCP entry removal; test artifact 8b4baf44 (frame-test) is orphaned.
