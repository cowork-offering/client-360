# Autonomous loop report, 2026-09-01 (~07:15-08:40, founder away)

Founder directives executed: "fix the brain, both rooms, loop until solved" + the entry
choreography spec. Companion docs: BRAIN-PROOF-SCRIPT / BRAIN-PROOF-RESULTS (the joint drive
that motivated this), BRAIN-FIRST-SPEC (build contract), design/ENTRY-CHOREOGRAPHY-INTENT +
design/proposals/entry-choreography-addendum.md (the taste proposal).

## Shipped (main, pushed; artifact 91b5e835 republished twice)

1. **de8e4dd - brain-first lane inversion** (label `brain-first`), both rooms:
   - Reads local-first, INCLUDING pre-route (F1, F2-read fixed): covenants/guarantors/
     collateral answered from the bundle, no blind brain.
   - Dispatch: provably-clean parses keep the instant fast lane; everything fuzzy (unparsed,
     partial, multi-clause, qualifier conflict) goes to the desk with an enriched envelope v2
     (read blocks + 6-turn thread digest + fileable map, 10KB cap, index-name prohibition).
     Fixes F3/F6 routing; staging still ONLY via restateProposal + parser + human confirm.
   - Qualifier filter (F4) + magnitude advisory (F5) as channel-independent safety layers.
   - Evidence: 2323/2323 tests (verified independently, not just agent-claimed), tsc clean,
     engine fence tree 91c751e4 identical, probe A/B vs previous build: identical FAIL set
     (98/98), rim violations 0.
2. **c896670 - entry choreography** (label `entry-choreo`), both rooms, PROPOSAL status:
   - question:on alone -> route bind -> identity earns in (question leaves) -> detail earns in
     (6 facilities staggered; earlier tiers faded, summonable). Live-proven in a real browser
     incl. staging still working after the ritual (2 delta cards on a typed instruction).
   - 2341/2341 tests, tsc clean, fence identical, contract files untouched. Proposed rule 68 in
     design/proposals/entry-choreography-addendum.md - founder mints or kills.

## Evidence notes (do not relearn)

- Probe baseline reality: compare.mjs reports **98 pre-existing FAILs** on BOTH the sealed
  hartwell-verdict build and everything after - dummy-baseline vs live-data divergences (hero
  names, meter timings, 4-seat arc). The handoff's "9 directed leaves" accounting does not
  match this tool output; gate discipline used here = **A/B delta (no NEW fails) + rim
  violations 0**, which is the honest machine gate until the baseline story is reconciled.
- The workroom.ritual probe module now dies at the route question (null .wk-pkg click): the
  pre-intent probe cannot answer "what do you want to do". 14 missing leaves are a probe-module
  artifact of the founder's own intent, replaced by 3 proposed leaves in the addendum. The live
  Playwright drive above is the interim evidence.
- cmdk.lens.topbarFilterRestored flaked once (2/3 true, stable true on rerun) - sampling flake,
  same family as haloBoxSizeStablePx.
- Artifact publish guard: republishing the pinned artifact from a NEW session requires an
  Artifact read of the URL first (refusal hands you the live copy; the live copy = payload +
  frame-runtime wrapper, ~14KB, explains byte diffs vs the assembled file).
- sync-plugin-assets.mjs lives at REPO ROOT scripts/, not app/scripts/.

## NOT done (founder-gated, in order for his return)

1. Live brain round trip on the FINAL build (fresh claude.ai session, connectors attached):
   rerun knowledge/BRAIN-PROOF-SCRIPT-20260901.md - P2, P5, P6, P7, P10, P11 must flip; P4/P8
   still unobserved from the first drive.
2. Taste confirm on the entry choreography (label entry-choreo on the pinned artifact), then
   mint-or-kill the addendum (rule 68 + 3 probe leaves + INTENT-OVERRIDES entry).
3. The everything-plan org probe + Relationship-room five routes (joint, org writes).
4. Judgement calls awaiting his word: greeting bubble retires on cmdk entry (one-line revert);
   relationship-room tier mapping (scope brief = identity); magnitude-advisory correction is a
   decimal-shift heuristic ($900M -> $90M).

Org state: untouched all loop (verified 1 package / 7 loans before and after the joint drive;
no filings occurred). Worktrees brain-first + entry-choreo can be removed (branches merged).
