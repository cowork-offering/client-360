# Customer 360 — Functionality Baseline (audited 2026-08-20, Archy seat)

Purpose: the plan-of-record inventory of every tool across every layer, each verified TODAY against
its source, so WS0-WS5 build on ground truth. Method per layer is named. This doc supersedes any
tool count in older docs (incl. the "24 tools" claim — actual is **23**, see D1).

## Layer 0 — the org (ground truth)

Retrieved 2026-08-20 via `sf project retrieve -m McpServerDefinition:Customer360` + Tooling API
class query, org `bankinggpt`.

**`Customer360` McpServerDefinition exposes exactly 23 tools:**

### Reads (9) — all List<Request> → List<Response>, run-as-user

| Tool | What it does |
|---|---|
| `Customer360Portfolio` | The book: accounts[] by TCE, bookTotals rollups, signals (covenants due/breached, maturities) — the worklist source |
| `Customer360SearchAccounts` | Name-based account lookup (open-by-name). No `note` field (known) |
| `Customer360Snapshot` | One relationship: package stage (managed + local cm_ stage for DQ), risk grades, package id (deep-link anchor) |
| `Customer360RelationshipGraph` | Connections + Legal Entities: ownership, guarantors, borrowing structure |
| `Customer360Exposure` | Facilities + collateral: commitments, outstanding, pledged-share math, coverage ratio (org-computed, honest nulls), collateralId anchors for valuation writes |
| `Customer360Covenants` | Covenant package: thresholds, actuals, status, attachedLoans, complianceId anchor (absent blocks assessment) |
| `Customer360Opportunities` | Open opportunities (whitespace) |
| `Customer360StructuralSignals` | Early-warning: modification clustering, guarantor signals, maturity window |
| `Customer360ActionHistory` | The relationship's action audit trail (client requests, concluded analyses, next steps) |

### Write pairs — stage = zero domain DML → plan/planHash/single-use decisionToken; execute = exactly `{idempotencyKey, stagingId, planHash, decisionToken, approverUserId}`, approver == running identity, verification re-queries

| Action | stage | execute | Domain writes on execute |
|---|---|---|---|
| Collateral valuation (incl. bulk items[]) | ✅ | ✅ | `LLC_BI__Collateral_Valuation__c` (live-proven: CV-0000000002/3) |
| Service request | ✅ | ✅ | Case |
| Annual review | ✅ | ✅ | `LLC_BI__Review__c` (never at Complete — A33.4.6) |
| Risk-rating review | ✅ | ✅ | Review + rating fields (live-proven: R-4) |
| New facility (package-first + borrowing structure, two-invocation resume) | ✅ | ✅ | Package/Loan/Entity-Involvement tree (live-proven e2e, then tree-deleted) |
| Covenant review | ✅ | ✅ **GATED** | Compliance row — **fires unrecallable approval email at robert.mcclaren@outlook.com. Founder-gated, never demo-fire** |
| Loan modification (facilityIds[], package-anchored) | ✅ | ❌ stage-only | By design (LV06/Booked wall) |
| Renewal (facilityIds[]) | ✅ | ❌ stage-only | By design + ⚠️ clone re-probe (Loan_Collateral_Aggregate) required before EVER unblocking |

Engine (not tools): `C360ActionStaging`, `C360Plan`, `C360WriteGuard` (transition allowlist),
`C360Facilities`, `C360Picklist`. Test classes: 12 (suite target 170/170; re-run = WS0).
The other Stage*/Execute* classes in the org (StageConfiguration*, ExecuteESign*, etc.) are
**pre-existing org build — never ours, never touched.**

## Layer 1 — claude.ai connector ("Customer 360")

Session tool list 2026-08-20: **23 tools, 1:1 with the server manifest.** ✅ No drift.

## Layer 2 — published artifact (both URLs)

- Capabilities: `{mcp}` contract 0.1.15. Recorded manifest (HANDOFF-2026-07-27): "21 tools,
  execute_covenant_review deliberately excluded" + IDB Gateway (3) + M365 (1: outlook_email_search).
- **D3 (open):** 23 server tools − 1 exclusion = 22 ≠ 21. The recorded count predates the Jul 28-29
  waves. Exact stored manifest must be re-verified and re-stated at the next republish. Capabilities
  carry forward when omitted, so today's live behavior is the stored (July) manifest.
- App source (repo HEAD): references all 23 real tools (execute_covenant_review 1 gated ref) —
  worklist, tickets, staged plans, confirm flows all wired.

## Layer 3 — Cowork plugin (client-360 v0.4.2, marketplace-served)

- Marketplace entry present (`cowork-plugins`, git-subdir repo root) → bundle/template/render are
  ALWAYS repo-fresh. ✅
- **The prose is the stale part (D2):** plugin.json says "8 read tools"; SKILL.md (405 lines)
  drives Portfolio + the six detail reads + Boom + optional M365 intake and contains **zero**
  mention of any stage_/execute_ tool, SearchAccounts's role, or ActionHistory in its fetch
  sequence. The plugin surface today is a READ cockpit with baked-in write UI; the agent-side
  write methodology (stage → present plan → human confirms → execute) is undocumented in the skill.
  Closing this = WS3 (v0.5.0), and it is the precondition for "natural chat" in Cowork.

## Layer 4 — KYC & Onboarding (the WS1 target)

- Cockpit: KYC & Onboarding zone EXISTS and self-gates honestly: `Customer360Onboarding` read is
  planned-not-built (contract.ts marks it PENDING/sample-only); onboarding write actions render the
  full flow then say "filing will light up when Customer360Write ships." No fake writes. ✅ honest,
  ❌ not live.
- IDB Gateway (staging) already carries the KYC tool set as connector tools TODAY:
  `get_customer360`, `get_customer`/`get_customer_names`, `get_addresses`, `stage_address_change` /
  `approve_stage_address_change`, `stage_name_change` / `approve_name_change`,
  `check_sanctions_name`, `check_adverse_media`, `assert_kyc_risk_address_change_for_high_risk_countries`,
  `create/update_risk_alert`, `create/get/update_service_request`, `create/update_task` +
  `get_task_queue`, documents (`get_documents`, `save_documents`, `doc_extract_r`,
  `extract_address_from_document`), audit (`get/store/update_change_audit`),
  `list_customer_interactions`, `capture_channel_interaction`, `get_llm_response`, db/graph/sparql
  query tools. (~36 tools; staging env, uptime unproven — envelopes must be observed before ANY
  shape is pinned. The law applies.)
- **WS1 = wire the zone to these**: same stage→approve governance as the nCino side, both surfaces.

## Layer 5 — repo + distribution state

| Check | State 2026-08-20 |
|---|---|
| `cowork-offering/customer-360` main | `5d4e31e` == local main ✅ (push auth: GH_CONFIG_DIR gh-claudeeshannon) |
| Marketplace `cowork-plugins` | client-360 **0.4.2** listed, alongside credit-memo-reinvented + boom-financials ✅ |
| Banksy clone | synced `5d4e31e`, brain docs current ✅ |
| Legacy `origin` (weareconnectry/customer360-cockpit) | 42 commits stale; archive-or-sync decision OPEN (housekeeping) |
| Artifact URLs | byte-verified vs `20bf17a` on Aug 14; knowledge-only commits since (no bundle change) — re-verify at next publish |

## Deltas the baseline surfaces (each gets closed, none silently)

| # | Delta | Close in |
|---|---|---|
| D1 | Docs say "24 tools"; server exposes **23** | STATUS/handover prose fix at v0.5.0 |
| D2 | Plugin prose: "8 read tools", zero write methodology | WS3 |
| D3 | Artifact stored capabilities manifest = July state (recorded "21"); arithmetic says 22 expected | Re-verify + restate at next republish (WS4) |
| D4 | `Customer360Onboarding` read + onboarding writes = planned/baked only | WS1 decides: org-side tools vs IDB gateway wiring (gateway is the plan) |
| D5 | Legacy public repo 42 behind | Housekeeping decision (Fabian) |
| D6 | Org runtime state (permset, Hartwell 18/18, suite, July receipts) inventoried here but NOT re-verified | WS0 campaign — this baseline is inventory, not verification |

## What "correct setup" means going forward (the contract)

One repo (cowork-offering/customer-360) → one bundle → three surfaces (artifact URLs, Cowork
plugin, repo). Marketplace serves the plugin repo-fresh. The server manifest (Layer 0) is the
single source for tool truth; every other layer states its tool list BY DERIVING from it, never
free-hand. Any layer that can't be derived-verified gets a delta row here until it can.
